[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "java -Xmx4g -jar otp.jar --load graphs/london --serve"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
