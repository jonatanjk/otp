[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "java -Xmx2g -XX:+UseContainerSupport -jar otp.jar --load graphs/london --serve"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
